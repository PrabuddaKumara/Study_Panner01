import 'package:flutter/material.dart';

class AssimentPage extends StatelessWidget {
  const AssimentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Assiment  Page"),
      ),
    );
  }
}